---
name: omarchy-look-port
description: "Omarchy (DHH) look ported to the Asahi Hyprland setup — theme engine, configs, how to switch themes and roll back"
metadata: 
  node_type: memory
  type: project
  originSessionId: 5d2f44f1-25f3-420b-8bd5-47b5079ace80
---

Ported the **visual layer** of Omarchy 3.8.2 (DHH's Arch/Hyprland setup) onto the existing [[system-asahi-hyprland]] install on 2026-05-29. NOT a full Omarchy/Omadora install (those assume Arch/fresh Fedora and may lack aarch64 packages) — only the architecture-independent look + theme system.

**What was done:**
- Omarchy repo (tarball, no git on this box) lives at `~/.local/share/omarchy` (its `bin/`, `default/`, `themes/`).
- `~/.bashrc` exports `OMARCHY_PATH` + adds `$OMARCHY_PATH/bin` to PATH + `starship init`.
- Theme engine = pure bash+sed (`omarchy-theme-set-templates`): `colors.toml` + `default/themed/*.tpl` → `~/.config/omarchy/current/theme/<app>.{css,toml,conf,theme}`. Active theme written to that dir; name in `current/theme.name`.
- App configs @import the generated theme files: waybar `style.css`, kitty `kitty.conf` (include prepended), mako (`~/.config/mako/config` symlinked to themed `mako.ini`), btop (`~/.config/btop/themes/current.theme` symlink), starship.
- `~/.config/hypr/hyprland.conf` now mirrors Omarchy's layered structure for **1:1 keybindings** (2026-05-29). Sources Omarchy defaults: `bindings/{media,clipboard,tiling-v2,utilities}.conf` (~192 binds — full Omarchy set: universal copy/paste/cut, window groups, scratchpad, swap, workspace 1-10/Tab, monitor scaling cycle, emoji/color picker, nightlight, notifications, lock Super+Ctrl+L, etc.), `envs.conf`, `looknfeel.conf`, theme `hyprland.conf`. Then user overrides: `monitors.conf` (eDP-1 scale 2.0), `input.conf` (us-intl + natural-scroll touchpad + 3-finger gesture), `bindings.conf` (Omarchy app-launch binds, copied verbatim — Super+Return terminal via xdg-terminal-exec→kitty, Super+Shift+Return browser, webapps, etc.), `autostart.conf` (Fedora-adapted services).
  - **Menu Logout fix (2026-05-29):** Session is launched by **plasmalogin** (Plasma/SDDM greeter `--start Hyprland`), NOT uwsm. Omarchy's `omarchy-system-logout` used `uwsm stop` → did nothing → logout from the menu (System→Logout) failed. Patched `~/.local/share/omarchy/bin/omarchy-system-logout` (backup `.bak-uwsm`) to close windows then `hyprctl dispatch exit` (returns to greeter). Restart/Shutdown (systemctl reboot/poweroff) and Lock (hyprlock) were fine.

**Deliberately NOT sourced:** `windows.conf` (Hyprland 0.53+ syntax — breaks 0.51.1) and Omarchy's `default/hypr/autostart.conf` (uwsm + omarchy-first-run/Arch-specific) — autostart kept custom.
  - `env = PATH, /home/remdph/.local/share/omarchy/bin:...:/var/lib/flatpak/exports/bin` set at top of hyprland.conf so keybind-launched `omarchy-*` scripts resolve (applies at Hyprland start).
  - `~/.config/xdg-terminals.list` = kitty.desktop, Alacritty.desktop (for Super+Return / xdg-terminal-exec).
  - Key differences from old custom binds: close is now Super+W (was Super+Q), Super+Escape = system menu (was hyprlock; lock moved to Super+Ctrl+L), Super+Space = launch apps (walker). App-launch binds for uninstalled apps (nautilus, spotify, signal, 1password, typora) fail silently; webapp binds need a Chromium browser (only firefox installed).
  - Backup of pre-1:1 hypr dir: `~/omarchy-backup-hypr-20260529-125320/`. Look via looknfeel (rounding 0, blur, shadows, animations). Default theme: tokyo-night.
- Installed: JetBrainsMono Nerd Font (manual, `~/.local/share/fonts`, not in Fedora repos), starship (binary, not in repos).

**Asahi/0.51.1 caveat:** Omarchy targets a newer Hyprland. Commented out `scrolling{column_width}` and `misc:on_focus_under_fullscreen` in `default/hypr/looknfeel.conf` (don't exist in 0.51.1). Re-check after any Hyprland upgrade or omarchy re-extract.

**Greeter + locker temáticos (2026-05-29):** locker `~/.config/hypr/hyprlock.conf` reescrito para usar colores temáticos + `current/background` (bak `.bak-static`). Login **plasmalogin** (=SDDM forkeado, greeter Plasma 6 con wallpaper plugin) tematizado vía dir compartido propiedad del usuario `/var/lib/omarchy-greeter/{background,kdeglobals}` (legible por uid plasmalogin 985); `/etc/plasmalogin.conf` apunta `[Greeter][Wallpaper][org.kde.image][General] Image=` ahí (bak `.omarchy-bak`); `/var/lib/plasmalogin/.config/kdeglobals` es symlink a ese (bak `.omarchy-bak`). Generador `~/.local/bin/omarchy-greeter-sync` (lee `current/theme/colors.toml`, sin root); auto-update vía hook `~/.config/omarchy/hooks/theme-set.d/greeter`. Aplica al siguiente arranque del greeter (logout/reboot), no en vivo. Detalle completo en `~/omarchy-asahi-setup.md`.

**Switch theme:** `omarchy-theme-set <name>` (list: `omarchy-theme-list`). 19 themes available. Note: `omarchy-theme-set` calls restart helpers (swayosd/walker/ghostty) that aren't installed — harmless stderr; configs still generate. To avoid noise, regenerate manually: copy theme to `current/next-theme`, run `omarchy-theme-set-templates`, mv to `current/theme`, then `hyprctl reload && pkill -x waybar && waybar &` + `makoctl reload`.

**Compiled from source (2026-05-29, aarch64)** — installed to `~/.local/bin`:
- **elephant** (Go, abenz1267/elephant v2) = walker's backend daemon. Binary + 10 provider plugins (.so) in `~/.config/elephant/providers/`: desktopapplications, calc, files, clipboard, menus, symbols, runner, providerlist, websearch, unicode. Runs as `elephant.service` (systemd user) — unit at `~/.config/systemd/user/elephant.service`, ExecStart patched to absolute `%h/.local/bin/elephant` (PATH issue). Socket at `$XDG_RUNTIME_DIR/elephant.sock`. Go plugins must be rebuilt with the SAME go toolchain + module versions as the elephant binary, all from one checkout — if elephant is rebuilt/updated, rebuild ALL providers too.
- **walker** (Rust 2.16.2, abenz1267/walker — now Rust, not Go). Config `~/.config/walker/config.toml` = Omarchy's (theme=omarchy-default, prefixes / . : = @ $, theme-switcher menu). Started via `walker --gapplication-service` (exec-once); opened with `walker`. Bound to Super+Space and Super+R.
- **swayosd** (Rust/meson, ErikReider/SwayOSD 0.3.1) — swayosd-server + swayosd-client only (skipped privileged libinput-backend). Server via exec-once; media/brightness keys in hyprland.conf call `swayosd-client --output-volume|--brightness`.

**Build deps installed via `pkexec dnf`** (sudo needs password — user said use pkexec): golang, rust, cargo, meson, gtk4-devel, gtk4-layer-shell-devel, pulseaudio-libs-devel, scdoc, sassc, git, poppler-glib-devel, protobuf-compiler (walker needs protoc), dbus-devel, libinput-devel, libevdev-devel (swayosd), qalculate (qalc, for calc provider), fd-find (files provider). Toolchain pins (rust-toolchain.toml nightly for swayosd) ignored — no rustup, Fedora stable rustc 1.95 works; deleted swayosd's rust-toolchain.toml.

`~/.config/environment.d/10-omarchy.conf` exports OMARCHY_PATH + prepends `~/.local/share/omarchy/bin` and `~/.local/bin` to PATH for the graphical session (so walker's exec-once + theme-switcher menu reach omarchy-* scripts). Applies on next login.

ghostty still substituted by kitty.

**Spotify on Asahi ARM (2026-05-29):** Spotify Web player (as a Chromium webapp or tab) does NOT work — needs Widevine DRM, which has NO Linux ARM64 build (Chromium's WidevineCdm dir is empty). Native Spotify client also isn't built for ARM Linux. The Flatpak ncspot (io.github.hrkfdn.ncspot) crashed with a crossterm TTY error ("Can not disable mouse capture": I/O error) — flatpak sandbox TTY issue, not audio/DRM. Solution: built **ncspot natively** via `cargo install ncspot --no-default-features --features pulseaudio_backend,cover,notify --root ~/.local` → `~/.local/bin/ncspot` (1.3.4, pulseaudio backend → PipeWire). Launcher `~/.local/share/applications/ncspot.desktop` runs `kitty --class=ncspot -e ncspot` (real TTY, no sandbox). **Requires Spotify Premium** (librespot limitation) — user has Premium. **CRITICAL build flag:** ncspot's cursive TUI needs a terminal backend feature — building with `--no-default-features` WITHOUT `crossterm_backend` makes cursive fall back to the Dummy backend (logs "No built-in backend, falling back to Dummy backend"), so ncspot connects/authenticates/inits audio fine but the TUI never renders and it exits immediately (looks like "Connecting... then closes"). Correct build: `cargo install ncspot --no-default-features --features crossterm_backend,pulseaudio_backend,notify --root ~/.local --force` (the `cover` feature is optional and needs ueberzug — omit it). Build deps: ncurses-devel + openssl-devel/pulseaudio-libs-devel/dbus-devel (already present). Launcher `~/.local/share/applications/ncspot.desktop` = `kitty --class=ncspot -e ncspot`. **Playback issue was NOT ncspot** — the error `Audio Sink Error Connection Refused: <PulseAudioSink> Timeout` was because the SYSTEM audio output was stuck on the **Headphones port** (which was "not available" — nothing plugged in) and the **speaker sink didn't exist** (pw-play of a short wav also hung). Fix: `systemctl --user restart wireplumber` → recreated the Asahi speaker DSP chain `audio_effect.j414-convolver` (the J414/MBP14" speaker convolver) as the default sink; audio then plays. This stuck-on-headphones state can recur after reboot/suspend (known Asahi audio routing issue) — workaround is restarting wireplumber. speakersafetyd is active. Alternative for free accounts: Spotube (Flathub, plays via YouTube).

**VSCode on Asahi 16K (2026-05-29):** The **Flatpak** `com.visualstudio.code` CRASHES with SIGSEGV on this 16K-page kernel — its Electron binary has ELF LOAD segments aligned to <16K, so the loader faults before exec (the `--reuse-window @@ %F @@` path in its .desktop made it worse). Root cause = Asahi 16K-page ELF alignment, documented at asahilinux.org/docs/sw/broken-software. **Correct install = Microsoft's official RPM**, NOT Flatpak: added repo `/etc/yum.repos.d/vscode.repo` (baseurl https://packages.microsoft.com/yumrepos/vscode) + `dnf install code`. The native RPM (1.122.1) has all aarch64 binaries aligned to 0x10000 (64K) — only 3 unused *x86_64* bundled binaries are 4K — so it runs natively with ZERO crashes, no muvm needed. `code.desktop` Exec is clean (`/usr/share/code/code %F`) so it launches fine from walker. Uninstalled the Flatpak (`flatpak uninstall --user --delete-data com.visualstudio.code`) and removed its override .desktop. Also installed `muvm` (0.5.1, Fedora Asahi's 4K-page microVM for broken Electron/x86 apps) as a fallback — not needed for VSCode but kept for other apps. **General lesson: prefer native Fedora/MS RPMs over Flatpak for Electron apps on Asahi; Flatpak-bundled Electron is often mis-aligned for 16K pages.**

**Web App installer (2026-05-29):** Omarchy's menu → Install → Web App (`omarchy-webapp-install`) now works on Fedora. It creates a `.desktop` in `~/.local/share/applications` with `Exec=omarchy-launch-webapp <url>` + downloaded favicon. Two fixes: (1) installed `gum` (Fedora repo) — needed for the interactive name/URL prompts (works in a real terminal; the menu launches it via present_terminal so it has a TTY); (2) patched `omarchy-launch-webapp` (backup `.bak-pre-fedora`): its fallback browser was `chromium.desktop` but Fedora's is `chromium-browser.desktop` — fixed. It picks chromium when the default browser (firefox here) isn't a Chromium-family browser, then runs `chromium-browser --app=<url>` (Wayland-native, scales correctly). Verified end-to-end: .desktop + favicon created, launches a Wayland app window.

**Chromium HiDPI fix (2026-05-29):** Chromium (Fedora `chromium-browser`, aarch64) ran under XWayland → ignored the 2.0 monitor scale → tiny UI. Fedora's chromium does NOT read `~/.config/chromium-flags.conf` (that's Arch); its launcher sources `/etc/chromium/chromium.conf` (sets `CHROMIUM_FLAGS`, overridable by `$CHROMIUM_USER_FLAGS`). Added to `/etc/chromium/chromium.conf` (via pkexec): `CHROMIUM_FLAGS="--ozone-platform=wayland --ozone-platform-hint=auto --enable-features=TouchpadOverscrollHistoryNavigation"` → forces native Wayland (respects integer scale 2.0) + 2-finger back/forward. Verified `xwayland=False` after. Note: Google Chrome / Brave-official-repo / Edge have NO Linux aarch64 build at all; on ARM use chromium (dnf) or Brave/Vivaldi/ungoogled-chromium (Flathub aarch64).

**Widevine DRM installed (2026-05-29):** Used the official **AsahiLinux/widevine-installer** (by marcan; patches the ChromeOS arm64 Widevine CDM for vanilla/16K Linux). Installed CDM v4.10.2662.3 to `/var/lib/widevine/libwidevinecdm.so`, symlinked to `/usr/lib64/chromium-browser/WidevineCdm`, Firefox prefs + `/usr/lib/environment.d/50-gmpwidevine.conf` (MOZ_GMP_PATH) configured. The CDM version is 16K-native (installer said "supports your page size, didn't have to weaken memory permissions"). Needs `squashfs-tools` (unsquashfs), glibc≥2.36 (have 2.43). **Requires logout/login** to take effect. Enables DRM streaming (Netflix, Spotify Web, Disney+, etc.) in Chromium AND Firefox on this aarch64/16K Mac. (So Google Chrome itself can't be compiled — closed source — but Chromium + this Widevine gives the same streaming capability; no need to compile Chromium, the Fedora package already works.)

**Install menu de-Arch'd (2026-05-29):** Omarchy's menu → Install → Package used `omarchy-pkg-install` (pacman+fzf) and AUR used `omarchy-pkg-aur-install` (yay+fzf). Replaced with Fedora equivalents (mapping: pacman→dnf, AUR→Flathub + COPR). Edited `omarchy-menu` show_install_menu (backup `omarchy-menu.bak-pre-fedora-install`): entries now `Package (dnf)` → `pkg-install-dnf`, `Flatpak` → `pkg-install-flatpak`, `COPR` → `pkg-install-copr`. Those 3 scripts in `~/.local/bin` mirror the fzf+preview UX: dnf (`dnf repoquery --available` → `sudo dnf install`), flathub — reads the LOCAL appstream catalog (`~/.local/share/flatpak/appstream/flathub/*/active/appstream.xml.gz`, parsed in ~1s for 2714 apps) instead of `flatpak remote-ls` which fetched Flathub metadata over the network each time (~2 min → picker hung on "0/0"). Ran `flatpak update --user --appstream -y flathub` once to populate it; the script auto-refreshes it in the background if >7 days old. Installs via `flatpak install --user -y flathub <appid>`, copr — SEARCH-based: prompt term → query COPR API (`https://copr.fedorainfracloud.org/api_3/project/search?query=`) → fzf the matching projects → `sudo dnf copr enable` selected → fzf its packages → install. COPR has no global package catalog (unlike dnf/flatpak) so it's search-then-enable; COPR menu entry icon = 󰏖 (Package/Flatpak entries kept the 󰣇 glyph). Installed `fzf` (was missing); added Flathub as `--user` remote (only `fedora` OCI remote existed). Original omarchy-pkg-* scripts left untouched.

**Control TUIs (Super+Ctrl+A/B/W) (2026-05-29):** wiremix (audio) installed from Fedora repo; bluetui (bluetooth, Rust v0.8.0) built via `cargo install bluetui --root ~/.local` → ~/.local/bin/bluetui; wifi = nmtui (installing NetworkManager-tui upgraded NM to 1.56.1, network stayed fine). impala NOT used — it needs iwd but this host runs NetworkManager. Super+Ctrl+W rebound in bindings.conf (`unbind`+bindd) to `omarchy-launch-or-focus-tui nmtui`; audio/bluetooth keep Omarchy's launch scripts (which call wiremix/bluetui).

**Screenshots (2026-05-29):** Omarchy's editor is **satty** (annotation tool, com.gabm.satty) + slurp + grim. Installed satty 0.20.0 from solopasha COPR. SUPER+SHIFT+4 (macOS Cmd+Shift+4) bound in bindings.conf (via `unbind = SUPER SHIFT, code:13` which was Omarchy's move-to-ws-4) to `~/.local/bin/screenshot-area`: slurp region → grim to `~/Pictures/Screenshots/Screenshot <date>.png` → opens satty for annotation (Enter = save-to-clipboard,save-to-file,exit; Esc = exit keeping original). Omarchy's PRINT key (omarchy-capture-screenshot) now also works since satty is present. grim/slurp/wl-copy were already installed. These TUIs launch via `xdg-terminal-exec --app-id=org.omarchy.<name>` → kitty honors it as window class. To make them **float + center** like Omarchy (whose `apps/system.conf` uses 0.53+ `windowrule = float on, match:tag` syntax that 0.51.1 rejects), wrote `~/.config/hypr/windowrules.conf` with 0.51-compatible v2 rules: `windowrulev2 = float/center/size 875 600, class:^(org\.omarchy\..*)$`, sourced from hyprland.conf. Verified: opens 875x600 centered.

**Full omarchy-menu (config menu) wired (2026-05-29):** `omarchy-menu` (walker --dmenu based) bound in hyprland.conf — Super+Alt+Space (main), Super+Shift+Ctrl+Space (theme), Super+Ctrl+Space (background), Super+K (keybindings). Binds use a `$omenu = env OMARCHY_PATH=... PATH=...` prefix (hardcoded /home/remdph) so keybind-launched processes find omarchy-* without relogin. Main menu + theme picker (with live previews) verified working.

CRITICAL for the theme/background menus to show results: **elephant.service must have OMARCHY_PATH + PATH in its Environment** (the omarchy_themes.lua reads `os.getenv("OMARCHY_PATH")`, and the activate action runs `omarchy-theme-set`). Added `Environment=OMARCHY_PATH=%h/.local/share/omarchy` and `Environment=PATH=%h/.local/share/omarchy/bin:%h/.local/bin:...` to `~/.config/systemd/user/elephant.service`. Without it the picker shows "No Results". Themes live in `$OMARCHY_PATH/themes`, NOT `~/.config/omarchy/themes` (that's for user-custom themes).

**swayosd now a user service** (`~/.config/systemd/user/swayosd-server.service`, ExecStart=%h/.local/bin/swayosd-server) so `omarchy-restart-swayosd` works and theme switches restyle the OSD. Style symlinked: `~/.config/swayosd/style.css` → `~/.config/omarchy/current/theme/swayosd.css`. Hyprland autostart starts elephant + swayosd-server via `systemctl --user start`, walker via `--gapplication-service`. **swayosd-server startup race (fixed 2026-05-29):** the service was `WantedBy=graphical-session.target` so systemd started it too early — before WAYLAND_DISPLAY was ready — → `failed to initialize GTK Application` → restart-loop hit the rate limit → stayed `failed` → volume/mute keys (which call `omarchy-swayosd-client` → need the server) silently did nothing. Fix: `systemctl --user disable swayosd-server.service` (removed the premature auto-start); it's now started ONLY by the Hyprland exec-once `systemctl --user start swayosd-server.service`, which runs AFTER the `dbus-update-activation-environment --systemd WAYLAND_DISPLAY` exec-once imports WAYLAND_DISPLAY into the systemd user env (confirmed present), so GTK init succeeds. If volume keys ever die again: `systemctl --user restart swayosd-server.service`. `omarchy-theme-set <name>` now switches themes with zero errors end-to-end (verified kanagawa↔tokyo-night).

**Scrolling/flow layout — switched to hyprscroller (2026-05-29):** hyprscrolling (hyprwm) redistributes space on resize (others shrink) — by design, no PaperWM option. Replaced it with **hyprscroller** (cpiber fork, github.com/cpiber/hyprscroller, tag `0.51.1` which pins Hyprland commit 71a1216 = this exact build) — true PaperWM: resizing keeps other columns' sizes. Built from source (dawsers/master uses pre-0.48 API and fails on 0.51.1; cpiber fork has the new API): `git checkout 0.51.1 && make release` against hyprland-devel 0.51.1 → `~/.local/share/hyprland/hyprscroller.so`. Loaded via autostart `exec-once = hyprctl plugin load ~/.local/share/hyprland/hyprscroller.so`. Layout name = `scroller`. **IMPORTANT: hyprscroller only works as the GLOBAL layout** (`general:layout=scroller`); per-workspace assignment at runtime does NOT work (`scroller:cyclesize` → "invalid workspace", no re-tile) — so SUPER+L toggles GLOBALLY (user accepted this; true per-workspace isn't possible with this plugin). **Resize that keeps others = `scroller:cyclesize +1/-1`** (cycles the focused column through preset widths, others stay, row scrolls) — NOT resizeactive/mouse-drag (those redistribute). `~/.local/bin/hypr-resize wider|narrower` is a smart wrapper: scroller→cyclesize, dwindle→resizeactive; bound to SUPER+`=`/SUPER+`-` (code:21/20) in bindings.conf. `hypr-layout-toggle` flips `general:layout` dwindle↔scroller. New-column width: `plugin:scroller:column_default_width` (default `onehalf`≈737px, felt thin) set to `twothirds` (≈985px) — applied via the autostart line `hyprctl plugin load … && hyprctl keyword plugin:scroller:column_default_width twothirds` (plugin config MUST be set via `hyprctl keyword` AFTER the plugin loads — putting `plugin:scroller:*` in the parsed config errors "config option does not exist" since the plugin loads later via exec-once). Note: `hyprctl getoption plugin:scroller:column_default_width` misleadingly shows the registered default (onehalf) even when the live value is twothirds — verify by opening a window instead. Values: onehalf/twothirds/threequarters/one etc. **scroller is now the DEFAULT layout, dwindle deactivated (2026-05-29):** can't set `general:layout = scroller` in the parsed config (plugin loads later via exec-once → parse error), so scroller is forced via `hyprctl keyword general:layout scroller` chained in the autostart plugin-load line, AND re-asserted after every theme reload by patching `omarchy-restart-hyprctl` (backup `.bak`) to append `hyprctl keyword general:layout scroller` + column_default_width (looknfeel.conf still has `layout = dwindle` which a reload would otherwise re-apply). SUPER+L no longer toggles — `hypr-layout-toggle` now just (re)asserts scroller (recovery key). Caveat: a MANUAL `hyprctl reload` (not via theme) reverts to dwindle until SUPER+L. Old hyprscrolling note below is superseded.

**Scroller config moved to STATIC block + default width ⅞ (2026-05-30):** Fixed two bugs in the scroller setup. (1) New-window width changed `twothirds`→`seveneighths` (⅞). (2) ROOT-CAUSE FIX correcting the earlier note above: it claimed `plugin:scroller:*` cannot go in parsed config and MUST be set via `hyprctl keyword` after load — that was wrong and caused the actual bug: **`keyword`-set values (column_default_width, column_widths, general:layout) REVERT to plugin defaults on any `hyprctl reload`** (e.g. `column_widths` fell back to the 4 defaults `onethird onehalf twothirds one`, losing the threequarters/seveneighths steps). A **static config block** in a sourced file DOES work — Hyprland queues plugin keywords and applies them when the plugin loads via exec-once — and crucially it's re-read on every reload, so it persists. Replaced the autostart `exec-once … && hyprctl keyword …` chain with: `exec-once = hyprctl plugin load …hyprscroller.so` PLUS a static block in `~/.config/hypr/autostart.conf`: `general { layout = scroller }` and `plugin { scroller { column_default_width = seveneighths; column_widths = onethird onehalf twothirds threequarters seveneighths one } }` (placed after looknfeel.conf in source order so it overrides `layout = dwindle`). The 6 cycle steps (SUPER+`=`/`-` → `scroller:cyclesize`) are ⅓ ½ ⅔ ¾ ⅞ 1. Also updated the two reinforcement scripts `omarchy-restart-hyprctl` (theme-reload hook) and `hypr-layout-toggle` (SUPER+L) from `twothirds`→`seveneighths` (now redundant but kept). Verified survives `hyprctl reload`: default ⅞, all 6 widths (set:True), layout scroller. The line-66 caveat "a MANUAL hyprctl reload reverts to dwindle until SUPER+L" no longer applies — the static block keeps it.

**(superseded) Flow/scrolling layout (2026-05-29):** Hyprland 0.51.1 core only has dwindle+master. The "flow" (scrolling) layout comes from the **hyprscrolling plugin** (hyprwm, now hosted at code.hyprland.org; github.com/hyprwm/hyprscrolling does NOT exist). Installed the prebuilt COPR package `hyprland-plugin-hyprscrolling` (solopasha, built against the same 0.51.1 → ABI-compatible, no compilation) → `/usr/lib64/hyprland/libhyprscrolling.so`. Loaded at startup via `exec-once = hyprctl plugin load /usr/lib64/hyprland/libhyprscrolling.so` in autostart.conf. Omarchy's `omarchy-hyprland-workspace-layout-toggle` (SUPER+L) does NOT work on 0.51.1 because `hyprctl activeworkspace -j` lacks the `tiledLayout` field — so wrote `~/.local/bin/hypr-layout-toggle` and rebound SUPER+L to it in bindings.conf (`unbind = SUPER, L` + bindd). Plugin config keys under `plugin:hyprscrolling:*` (e.g. column_width, default 0.5). **Toggle must use GLOBAL `general:layout` (2026-05-29 fix):** `hypr-layout-toggle` reads `hyprctl getoption general:layout -j` (.str) and flips `general:layout` dwindle↔scrolling. Earlier it set the per-workspace rule (`workspace N, layout:scrolling`) which returns "ok" but does NOT re-tile already-open windows (looked like "nothing changed"); the global `general:layout` change DOES re-tile live (windows snap to equal scrolling columns). Difference is most visible with 3+ windows. config `looknfeel.conf` keeps `layout = dwindle` as the default, so a hyprctl reload resets to dwindle. Decided AGAINST updating Hyprland (only 0.51.1 packaged for aarch64 in solopasha COPR; aquamarine 0.9.5 was hand-built on this host for the libdisplay-info 0.3 fix — updating would mean rebuilding the whole hypr* stack; high risk on the primary machine for little gain).

Note: in this dev environment, backgrounding `walker --dmenu` / `walker --close` from the agent shell intermittently returns exit 144 (walker grabs exclusive keyboard) — harmless, screenshots still succeed when grim runs in a separate step with stdin from /dev/null.

**Rollback:** backup of original ~/.config/{hypr,waybar,kitty} at `~/omarchy-backup-20260529-121246/`.
